import React from "react";
import { FormWrapper } from "./forgot_password";
import ResetPasswordForm from "@/components/reset_password_form";

const ResetPassword = () => {
    return (
        <FormWrapper>
            <ResetPasswordForm />
        </FormWrapper>
    );
};

export default ResetPassword;
